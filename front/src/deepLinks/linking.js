/* const config = {
    screen: {
        Home:{
            path: "home"
        },
        Profile: {
            path: "profile/:id",
            parse: {
                id: (id) => `${id}`
            }
        },
        Notifications: "notifications",
        Settings: "settings"
    }
}

const linking = {
    prefixes : [
        'app.scannerDni/edit/password'
    ],
    config,
}

export default linking */