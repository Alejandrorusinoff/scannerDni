/* import { createAction, createReducer } from "@reduxjs/toolkit";

const data = {
    "dni": null, 
    "ID": null,
}

export const setDataVinculation = createAction('SET_DATA_VINCULATION')
const dataVinculationReducer = createReducer(data,{
    [setDataVinculation]: (state, action) => action.payload
})

export default dataVinculationReducer */